CREATE TABLE foundFormData (
    itemId serial primary key,
    Title varchar(300),
    ItemDescription text, 
    DateFound varchar(300),
    TimeFound varchar(300),
    LocationFound varchar(300),
    ImageURL varchar(300),
    Email varchar(300)
);